#ifndef JS_MODULE_HPP
#define JS_MODULE_HPP

#include <string>

// Don't really need hpp file here, but just in case
//std::string wasm_hash256(const std::string& crIn);

#endif //ifndef JS_MODULE_HPP