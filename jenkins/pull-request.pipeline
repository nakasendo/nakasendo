pipeline
{
    agent { label 'master' }
    stages {
        stage('build on windows mvsc2017') {
            steps {
                echo 'Get all env variable'
                sh 'printenv'
                echo '----------  FINISH environment variables  -------------'
                sh 'pwd'
                sh 'pwd'
                sh 'pwd'
            }
        }
    }
}