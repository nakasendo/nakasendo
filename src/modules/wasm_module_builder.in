${WASM_SCRIPT_ARG_GETTER_CMD}
echo -- WASM Build output file : [${WASM_OUTPUT_ARG}]
${emcc_EXECUTABLE} ${WASM_LIST_CPP_FILES} -lssl -lcrypto -L ${OPENSSL_WASM_ROOT_CMAKE_DIR}/lib -I ${OPENSSL_WASM_ROOT_CMAKE_DIR}/include -I ${CMAKE_SOURCE_DIR}/src/core -I ${SDK_GENERATED_HPP_DIR} -std=c++11 -s WASM=1 -s EXPORT_ALL=1 -o ${WASM_OUTPUT_ARG}